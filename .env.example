# Frontend (Vite) env vars must be prefixed with VITE_
# Local dev API URL
# VITE_API_URL=http://localhost:5050/api

# Production API URL (Render)
VITE_API_URL=https://lakshmi-travels-api.onrender.com/api

# Server-side only vars (used by Express server)
MONGODB_URI=your-mongodb-connection-string
PORT=5050

# CORS/auth cookie origins (REQUIRED in production)
# Set these on the API server (Render) so cookies and CORS work on iOS browsers.
# CLIENT_ORIGIN should be your deployed frontend URL (Netlify)
CLIENT_ORIGIN=https://lakshmitravels.netlify.app
# SERVER_ORIGIN should be your API base origin (no /api suffix)
SERVER_ORIGIN=https://lakshmi-travels-api.onrender.com

# Optional: When deploying frontend, set this to your Render API service URL
# Example: https://lakshmi-travels-api.onrender.com/api
# VITE_API_URL=https://your-api.onrender.com/api
